---
- name: Configure Ruby and Bundler
  hosts: all
  become: true

  tasks:

  - name: Update repositories cache and install packages
    apt:
      update_cache: yes
      pkg:
        - git
        - ruby-full
        - ruby-bundler
        - build-essential
      state: present
